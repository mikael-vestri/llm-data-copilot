# Cursor rules

Project rules for [Cursor](https://cursor.com). See [Cursor Docs → Rules](https://cursor.com/docs/rules).

| Rule | When it applies |
|------|------------------|
| **project.mdc** | Always — project structure, how to run, conventions. |
| **agent-and-sql.mdc** | When editing `agents/**/*.py` or the main scripts in `scripts/` — agent behavior, RAG, SQL quality, grounding. |

For Claude Code users, equivalent context lives in `.claude/CLAUDE.md` and `.claude/skills/llm-data-copilot/SKILL.md`.
